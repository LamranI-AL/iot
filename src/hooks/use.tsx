// "use client";
// import { use } from "react";

// export default function Posts({
//   postsProps,
// }: {
//   postsType: Promise<{ id: string; title: string }[]>;
// }) {
//   const posts = use(postsProps);

//   return (
//     <ul>
//       {posts?.map((post) => (
//         <li key={post.id}>{post.title}</li>
//       ))}
//     </ul>
//   );
// }
